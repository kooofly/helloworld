'use strict';

function __$styleInject(css, returnValue) {
  if (typeof document === 'undefined') {
    return returnValue;
  }
  css = css || '';
  var head = document.head || document.getElementsByTagName('head')[0];
  var style = document.createElement('style');
  style.type = 'text/css';
  if (style.styleSheet){
    style.styleSheet.cssText = css;
  } else {
    style.appendChild(document.createTextNode(css));
  }
  head.appendChild(style);
  return returnValue;
}

__$styleInject("body{background:#ccc;color:#666}.circle{opacity:.5;width:50px;height:50px;border-radius:50%;-webkit-transform:rotate(7deg);transform:rotate(7deg);background:blue}",undefined);

var addArray = function (arr) {
    var result = arr.reduce(function (a, b) { return a + b; }, 0);
    return result;
};

console.log(); // replace 插件测试

function sayHelloTo (v) {
    return 'hello ' + v
}
var result1 = sayHelloTo('Jason');
var result2 = addArray([1,2, 3, 4]);

// Print the results on the page.
var printTarget = document.getElementsByClassName('debug__output')[0];

printTarget.innerText = "sayHelloTo('Jason') => " + result1 + "\n\n";
printTarget.innerText += "addArray([1, 2, 3, 4]) => " + result2;
//# sourceMappingURL=index.min.js.map
